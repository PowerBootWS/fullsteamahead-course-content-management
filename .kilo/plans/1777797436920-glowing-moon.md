# Plan: Rename Project Folder `fullsteamahead` → `fsa`

## Goal
Rename the folder from `fullsteamahead-course-content-management` to `fsa-course-content-management` and update all internal references to the old folder/project name.

---

## Important Distinction

The string `fullsteamahead` appears in two completely different contexts in this codebase:

1. **The project/folder name** (`fullsteamahead-course-content-management`) — this is what the user wants to change.
2. **An external service domain** (`fsachat.fullsteamahead.ca`) — this is the live tutoring agent website URL embedded in lesson descriptions. **This is NOT a project name reference and should NOT be changed.**

---

## Files That Need Updating (project name references)

### 1. `package.json` — Line 2
- **Current**: `"name": "fullsteamahead-course-content-management"`
- **Change to**: `"name": "fsa-course-content-management"`

### 2. `package-lock.json` — Lines 2 and 8
- **Current**: `"name": "fullsteamahead-course-content-management"` (appears twice)
- **Change to**: `"name": "fsa-course-content-management"`
- *Note: This file is auto-generated; it would also be corrected automatically by running `npm install` after the rename.*

### 3. `.claude/skills/course-import.md` — Lines 100, 132, 144
These are three `cd` path references to the project directory used in workflow examples:
- **Current**: `cd /home/debian/Projects/fullsteamahead-course-content-management`
- **Change to**: `cd /home/debian/projects/fsa/fsa-course-content-management`
- *Note: These paths were already stale — they used a capitalised `Projects` and lacked the `fsa/` subdirectory — so this is a correctness fix as well as the rename.*

---

## Files That Do NOT Need Updating

| File | Why Not |
|---|---|
| `src/course_builder.ts` line 21 | `https://fsachat.fullsteamahead.ca` is the live external service URL, not the project name |
| `imports/2B1/2026-04-07.json` | All 50+ `fullsteamahead.ca` occurrences are the same live service URL embedded in lesson iframes |
| `.claude/skills/course-import.md` line 65 | Same external URL |
| `CLAUDE.md` | No folder path references |
| `.mcp.json`, `.kilo/kilo.jsonc`, `.env`, `.env.example`, `tsconfig.json` | No references to the project name or path |

---

## Implementation Steps

1. Rename the folder on disk:  
   `mv /home/debian/projects/fsa/fullsteamahead-course-content-management /home/debian/projects/fsa/fsa-course-content-management`

2. Update `package.json` — change `name` field.

3. Update `package-lock.json` — change `name` field in the two places it appears (or just run `npm install` which will regenerate it).

4. Update `.claude/skills/course-import.md` — replace the three stale `cd` path lines with the correct new path.

---

## Summary

Only **3 files** require edits (4 if you count `package-lock.json` separately from `package.json`). The large number of `fullsteamahead` hits in the search results are all `fsachat.fullsteamahead.ca` URLs pointing to your live website — those are unaffected by a folder rename.
